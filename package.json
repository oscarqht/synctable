{
  "name": "synctable",
  "version": "0.1.0",
  "description": "Cross-browser tree backup and workspace synchronization utility",
  "type": "module",
  "scripts": {
    "build:native": "./scripts/build-dia-db-reader.sh",
    "predev": "bun run build:native",
    "dev": "electrobun dev",
    "prebuild": "bun run build:native",
    "build": "electrobun build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@types/lz4js": "^0.2.2",
    "electrobun": "^1.18.1",
    "lz4js": "^0.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/three": "^0.185.4",
    "typescript": "^7.0.2"
  }
}
